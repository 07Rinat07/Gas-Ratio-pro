# Архитектура QC

**Ревизия:** 1

Стабильная платформенная граница реализована в `core/qc` и предоставляется через `QCApplicationService`. Машиночитаемые коды `QC-*` не зависят от языка; пользовательский текст разрешается через каталоги i18n.
