# QC Architecture

**Revision:** 1

The stable platform boundary is implemented in `core/qc` and exposed through `QCApplicationService`. Machine-readable `QC-*` codes are language-independent; user-facing text is resolved through the i18n catalogs.
