# LAS Viewer Recent Session Navigation v147

Adds renderer-neutral persistence for the selected recent-session group, selected session, grouping mode, and page. The state is stored atomically with existing pin and collapse preferences and survives application restarts.
