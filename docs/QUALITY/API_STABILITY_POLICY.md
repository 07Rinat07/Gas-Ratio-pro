# API Stability Policy

Public application-service methods and serialized Dataset contracts are versioned interfaces. Removal or incompatible modification requires a documented migration, changelog entry and regression coverage. Compatibility aliases may be retained during a deprecation window.
