# LAS Viewer Recent Session Bookmarks v150

Adds persistent named bookmarks for recent LAS Viewer sessions.

- Bookmarks are stored in the existing atomic recent-session preferences file.
- Labels are user-defined and renderer-neutral.
- A bookmark can resolve directly to grouped navigation state.
- Removing a recent session also removes its bookmark metadata.
