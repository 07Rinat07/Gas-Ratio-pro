# LAS Recent Session Quick Navigation v148

Adds renderer-neutral resolution of a recent LAS session to its grouped Workbench page and item position.

Features:
- locate a session by stable session key;
- resolve group key, group index, item index, and page;
- optionally persist the resolved navigation state;
- focus the latest valid session;
- deterministic validation and missing-session results.
