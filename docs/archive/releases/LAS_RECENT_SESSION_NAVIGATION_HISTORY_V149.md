# LAS Recent Session Navigation History v149

Добавлена renderer-neutral история переходов по списку недавних LAS-сессий.

Поддерживаются:

- back/forward navigation;
- сохранение истории между запусками;
- удаление forward-ветки после нового перехода;
- защита от дублирования одинаковых состояний;
- сериализуемый контракт состояния истории.
