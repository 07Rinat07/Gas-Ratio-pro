# LAS Viewer Recent Sessions v138

Adds a renderer-neutral recent-session catalog for the future Workbench.

The catalog exposes compact autosave metadata, active-session marking,
deterministic session keys, selected-entry recovery, and safe activation.
Raw LAS data and render cache content are not included.
