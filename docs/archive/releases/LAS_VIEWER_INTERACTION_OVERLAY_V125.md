# LAS Viewer Interaction Overlay v125

Unified renderer-neutral composition of synchronized depth cursor and selection overlays.

The service keeps overlay geometry and styling outside UI adapters, supports track filtering,
serialized interaction state, non-printable primitives, and composition with the base render model.
