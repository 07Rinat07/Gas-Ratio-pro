# LAS Bookmark Trash Audit Export v159

Added renderer-neutral filtering and atomic JSON export for bookmark trash audit events.

Supported filters: action, session key, free-text query, time range, and result limit.
The export contract includes schema/version metadata, event count, and SHA-256 integrity metadata.
