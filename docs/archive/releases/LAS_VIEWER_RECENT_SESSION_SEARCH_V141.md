# LAS Viewer Recent Session Search v141

Adds renderer-neutral search and filtering for recent LAS sessions.

Supported filters: free-text query, project ID, LAS ID, pinned-only, active-only, and invalid-state inclusion.

Filtering is performed before limiting and preserves deterministic pinned-first ordering.
