# v224.0 — Report layout engine refactor

Single architectural change set:

- page-aware composite layout metrics;
- explicit readable SVG font sizes instead of CSS class scaling;
- A3 landscape engineering geometry;
- wider depth and curve tracks;
- stacked min/avg/max rows per track;
- interval-label suppression on crowded overview logs;
- thicker curves and grid hierarchy;
- shared output for PDF, DOCX, PNG and SVG;
- version bump to v224.0.
